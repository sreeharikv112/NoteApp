<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.noteapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning"
        >
        <activity android:name=".uicomponents.activities.viewnote.ViewNote"
            android:theme="@style/AppTheme.NoActionBar"
            />
        <activity
            android:name=".uicomponents.activities.landing.MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar"/>
        <activity android:name=".uicomponents.base.BaseActivity" />
        <activity
            android:name=".uicomponents.activities.makenote.MakeNoteActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".uicomponents.activities.enterpin.PinActivity"
            android:theme="@style/AppTheme.NoActionBar"
            />
        <activity android:name=".uicomponents.activities.settings.SettingsActivity"
            android:theme="@style/AppTheme.NoActionBar"
            />
        <activity android:name=".uicomponents.activities.setuppin.SetupSecurity"
            android:theme="@style/AppTheme.NoActionBar"
            />
        <activity
            android:name=".uicomponents.activities.splash.SplashActivity"
            android:configChanges="orientation|keyboardHidden|screenSize"
            android:theme="@style/FullscreenTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>